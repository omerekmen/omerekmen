# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Generated by paraglide-js on every lint, check and build.
# These are gitignored by project.inlang/.gitignore, but Prettier only reads
# the root .gitignore, so a fresh checkout would fail formatting on files
# nobody wrote.
project.inlang/.meta.json
project.inlang/README.md
